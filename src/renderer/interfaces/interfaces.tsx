export interface ICustomer {
  id?: number;
  lastName: string;
  firstName: string;
  street: string;
  city: string;
}

export interface IConfig {
  companyName: string;
  street: string;
  city: string;
}
